<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Gambar Bendera -->
    <ImageView
        android:id="@+id/flagImageView"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:contentDescription="@string/deks"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp" />

    <!-- Teks Skor -->
    <TextView
        android:id="@+id/scoreTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Score: 0"
        android:textSize="18sp"
        android:layout_below="@id/flagImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

    <!-- Tombol Bantuan (Help) -->
    <Button
        android:id="@+id/helpButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Help"
        android:layout_below="@id/scoreTextView"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="16dp" />

    <!-- LinearLayout untuk kotak huruf yang diisi jawaban -->
    <LinearLayout
        android:id="@+id/answerBoxLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_below="@id/helpButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp">

        <!-- TextView untuk kotak huruf akan dibuat dinamis dari kode di belakang -->
    </LinearLayout>

    <!-- Kotak Huruf untuk memilih huruf yang tersedia -->
    <LinearLayout
        android:id="@+id/letterBoxLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_below="@id/answerBoxLayout"
        android:layout_marginTop="16dp">

        <!-- TextView untuk kotak huruf akan dibuat dinamis dari kode di belakang -->
    </LinearLayout>

    <!-- Tombol Submit -->
    <Button
        android:id="@+id/submitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submit"
        android:layout_below="@id/letterBoxLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />
</RelativeLayout>
